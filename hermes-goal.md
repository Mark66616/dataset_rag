# hermes-goal: dataset-rag 从 POC 到 MVP 的路线图与全量解析

> 本文档写给项目作者本人，目的有两个：
> 1. **帮你真正理解这个项目** —— 每个节点"干什么、怎么做的、优缺点是什么"，让你能写进简历、扛住面试追问。
> 2. **给出 POC → MVP 的落地路线图** —— 哪些事必须做、按什么顺序做、为什么。
>
> 文中对每个节点的描述都基于实际代码（`app/` 目录），不依赖任何外部设计文档。

---

## 目录

1. [一句话定位](#1-一句话定位)
2. [整体架构与数据流](#2-整体架构与数据流)
3. [离线导入链路逐节点解析](#3-离线导入链路逐节点解析)
4. [在线问答链路逐节点解析](#4-在线问答链路逐节点解析)
5. [基础设施层解析](#5-基础设施层解析)
6. [现有技术选型：优点与缺点](#6-现有技术选型优点与缺点)
7. [POC → MVP 路线图](#7-poc--mvp-路线图)
8. [简历怎么用这个项目](#8-简历怎么用这个项目)
9. [面试高频追问与参考答案](#9-面试高频追问与参考答案)

---

## 1. 一句话定位

> 面向产品文档（PDF/Markdown）的 RAG 问答服务：离线把文档解析、切分、向量化后存入 Milvus；在线对用户问题做"商品名澄清 → 三路并行检索（向量 / HyDE / Web）→ RRF 融合 → Rerank 重排 → LLM 生成"的 LangGraph 工作流。

简历版（更精炼）：

> 基于 LangGraph 的产品文档 RAG 问答系统，支持 PDF/Markdown 离线入库（MinerU 解析、多模态图片摘要、BGE-M3 混合向量、Milvus 存储）与在线多路检索增强问答（商品名消歧、HyDE、MCP Web 搜索、RRF 融合、Rerank 重排、SSE 流式输出）。

---

## 2. 整体架构与数据流

```
┌─────────────────────────── 离线导入链路（app/import_process）───────────────────────────┐
│                                                                                          │
│  POST /upload (FastAPI)                                                                  │
│    → node_entry        判断文件类型（PDF/MD），设置解析开关                                │
│    → node_pdf_to_md    MinerU API 解析 PDF → Markdown（异步任务轮询）                      │
│    → node_md_img       扫描 MD 图片 → VLM 生成摘要 → 上传 MinIO → 替换引用                  │
│    → node_document_split  按标题切分 → 长切短合 → 生成结构化 Chunk                          │
│    → node_item_name_recognition  LLM 识别商品名 → 生成商品名向量 → 写入 Milvus item_names    │
│    → node_bge_embedding   BGE-M3 为每个 Chunk 生成稠密+稀疏向量                              │
│    → node_import_milvus  幂等写入 Milvus chunks 集合（按 item_name 清旧数据）               │
└──────────────────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────── 在线问答链路（app/query_process）─────────────────────────────┐
│                                                                                          │
│  POST /query (FastAPI)                                                                   │
│    → node_item_name_confirm  结合历史提取商品名 → 向量检索对齐 → 确认/反问/拒答              │
│    → (并发) node_search_embedding        普通混合向量检索                                  │
│              node_search_embedding_hyde  LLM 生成假设文档 → 向量检索                        │
│              node_web_search_mcp         百炼 MCP Web 搜索                                 │
│    → node_rrf        两路本地结果做倒数排名融合                                            │
│    → node_rerank     本地+Web 合并 → BGE Reranker 重排 → 动态 TopK                         │
│    → node_answer_output  组装 Prompt → LLM 生成 → 写回 MongoDB 历史 → SSE 推送              │
└──────────────────────────────────────────────────────────────────────────────────────────┘

存储：Milvus（chunks / item_names 两个集合）｜ MongoDB（会话历史）｜ MinIO（图片）｜ Neo4j（仅客户端，未使用）
```

---

## 3. 离线导入链路逐节点解析

### 3.1 node_entry —— 入口与文件类型判断

**干什么**：接收上传文件路径，判断是 PDF 还是 Markdown，设置对应的解析开关，并提取文件标题。

**怎么做的**（`node_entry.py`）：
- 校验 `local_file_path` 非空；
- 按后缀 `.pdf` / `.md` 设置 `is_pdf_read_enabled` / `is_md_read_enabled`，并填充 `pdf_path` / `md_path`；
- 提取无后缀文件名作为 `file_title`（后续商品名识别和切分都要用到）；
- 调用 `add_running_task` / `add_done_task` 更新任务状态（供前端轮询进度）。

**优点**：逻辑简单、职责单一；异常路径（不支持的类型）不抛错，仅记录警告，流程可继续（虽然继续了也没意义，见缺点）。
**缺点**：
- 只认后缀不认内容，改个后缀就能骗过它；
- 不支持的格式（如 `.txt`）不会终止流程，只是关掉所有开关，图会直接走到 END，任务状态却是 completed —— 会产生"假成功"。

### 3.2 node_pdf_to_md —— MinerU PDF 解析

**干什么**：把 PDF 上传到 MinerU 云服务解析为 Markdown，下载并解压结果。

**怎么做的**（`node_pdf_to_md.py`，三个 step）：
1. `step_1_validate_paths`：校验 PDF 存在、输出目录可写并自动创建；
2. `step_2_upload_and_poll`：调用 MinerU `/file-urls/batch` 获取签名上传链接 → PUT 上传（失败时强制指定 `Content-Type: application/pdf` 重试一次）→ 每 3 秒轮询 `/extract-results/batch/{batch_id}`，直到 `done`（10 分钟超时），拿到结果 ZIP 的下载链接；
3. `step_3_download_and_extract`：下载 ZIP → 清空旧的解压目录 → 解压 → 按优先级找目标 MD（同名 → `full.md` → 任意第一个）→ 统一重命名为 `{pdf名}.md` → 读取内容写入 state。

**优点**：异步轮询 + 超时 + 网络重试，工程处理较完整；批量接口一次拿签名链接和任务 ID，流程标准；对 5xx 做了重试区分。
**缺点**：
- **强依赖外部云服务**（MinerU API + API Token），离线场景、内网部署直接不可用；
- 轮询是同步阻塞的（`while True + time.sleep`），占用 API 进程线程；单文件最多占 10 分钟；
- 上传到本地磁盘再读入内存，大 PDF 会占内存峰值（文件全部 `f.read()`）；
- 无重试上限（只有超时），网络抖动可能白白等满 10 分钟。

### 3.3 node_md_img —— 图片摘要 + MinIO 上传

**干什么**：把 Markdown 里引用的图片用多模态大模型生成文字摘要，上传到 MinIO，并把 MD 里的本地图片引用替换为 `![摘要](MinIO URL)`。

**怎么做的**（`node_md_img.py`）：
1. `step_1_get_content`：读 MD 内容，确定图片目录（MD 同级的 `images/`）；
2. `step_2_scan_images`：遍历图片目录，过滤"支持格式 + 在 MD 中实际被引用"的图片，记录每张图在 MD 中的上下文（前后各 100 字符）；
3. `step_3_generate_summaries`：每张图 base64 编码 → 构造 LangChain 多模态 `HumanMessage` → 调用 VLM（`VL_MODEL`）生成 ≤50 字摘要；带限速（默认 9 次/分钟）防止触发 API 限流；
4. `step_4_upload_and_replace`：先清理 MinIO 上该文档的旧目录（幂等）→ 逐张 `fput_object` 上传 → 合并摘要和 URL → 正则替换 MD 中的 `![...](本地路径)` 为 `![摘要](URL)`；
5. `step_5_backup_new_md_file`：写为 `{原名}_new.md`，原文件不动。

**优点**：图片"摘要 + 对象存储"的思路很好——向量库只存文字摘要，问答时图片作为附加信息展示，避免图片直接参与向量化；限速、幂等清理、失败降级（返回默认"图片描述"）都考虑了。
**缺点**：
- 摘要质量直接决定图片的可检索性，而摘要只保留 50 字，信息损失大；
- 每张图一次 API 调用，**成本随图片数量线性增长**，限速 9 次/分钟意味着 100 张图要 11 分钟；
- 正则替换是"匹配文件名"的启发式方案，遇到 MD 里同名图片、图片路径带参数等情况会替换错；
- 上传失败静默降级（返回 `None`），图片会悄悄丢引用。

### 3.4 node_document_split —— 文档切分

**干什么**：把整篇 Markdown 按标题结构切成语义相对完整、长度适中的 Chunk（默认上限 2000 字符、下限 500 字符）。

**怎么做的**（`node_document_split.py`）：
1. 统一换行符（`\r\n` → `\n`）；
2. `step_2_split_by_titles`：逐行扫描，用正则 `^\s*#{1,6}\s+.+` 识别 1-6 级标题，**跳过代码块内的伪标题**（维护 `in_code_block` 状态），按标题切出章节；
3. 无标题时兜底为单个"无标题"章节；
4. `_split_long_section`：超过 2000 字符的章节用 LangChain `RecursiveCharacterTextSplitter` 二次切分，分隔符优先级为 段落(`\n\n`) → 换行 → 中文标点 → 英文标点 → 空格，子块带 `part` 序号和 `parent_title`；
5. `_merge_short_sections`：同父标题且不足 500 字符的相邻块合并，减少碎片；
6. 补齐 `parent_title` / `part` 字段（适配 Milvus schema），并把 `chunks` 备份到本地 `chunks.json` 供调试。

**优点**：标题感知切分 + 长切短合，比固定窗口切分质量高；代码块过滤是细节亮点；字段设计（`file_title`/`item_name`/`parent_title`/`part`）为后续检索过滤和溯源留好了口子。
**缺点**：
- 切分只依赖 Markdown 标题层级，对无标题的表格/列表文档质量一般；
- 2000 字符上限是硬编码默认值，未从配置读取（`max_len` 其实预留了但调用处没用）；
- 中文标点分隔符硬编码在代码里，换语言体系需要改代码；
- 标题行会重复出现在每个子块中（`title` 前缀 + 内容），浪费 token。

### 3.5 node_item_name_recognition —— 商品名识别与入库

**干什么**：让 LLM 从文档切片中识别出"这份文档讲的是哪个产品/型号"，为该商品名生成向量并写入 Milvus 的 `item_names` 集合（用于在线链路第一步的实体对齐）。

**怎么做的**（`node_item_name_recognition.py`）：
1. 取前 5 个切片（每个截断 800 字符、总上限 2500 字符）构造上下文；
2. 加载 `item_name_recognition.prompt` + 系统提示词，调用 LLM（非 JSON 模式）识别商品名，返回结果清洗（去空格/换行）；
3. 把 `item_name` 回填到每个 chunk（`chunk["item_name"] = ...`）；
4. 用 BGE-M3 为商品名生成稠密+稀疏向量；
5. 写入 Milvus `item_names` 集合：不存在则自动建集合（HNSW 稠密索引 + SPARSE_INVERTED_INDEX 稀疏索引），插入前按 `item_name` 删除旧数据（幂等）。

**优点**：商品名作为"业务主键"贯穿导入和查询两端，形成"先对齐商品、再检索文档"的两级检索结构，这是这个项目区别于通用 RAG 的核心设计；异常全部降级为 `file_title` 兜底，不中断流程。
**缺点**：
- **单个文档只识别一个商品名**（单数语义），一个手册讲多个型号时只能取一个；
- LLM 识别结果不稳定（同样的文件可能识别出不同名字），而商品名是幂等删除的键——同名不同写法会导致数据覆盖或重复；
- 识别只看前 5 个切片，商品名出现在文档后半部分时会漏掉；
- 商品名向量化用了与 chunk 相同的 BGE-M3，未做专门优化（如拼 context）。

### 3.6 node_bge_embedding —— 向量化

**干什么**：为每个 Chunk 生成 BGE-M3 稠密（1024 维）+ 稀疏双向量。

**怎么做的**（`node_bge_embedding.py`）：
- `get_bge_m3_ef()` 返回单例 embedding 模型（全局只加载一次）；
- 文本增强：拼接 `商品：{item_name}，介绍：{content}`（把商品名前置，利用 BERT 类模型对前 128 token 注意力最强的特性）；
- 每批 5 条生成，单批异常不中断整体（保留原数据继续）。

**优点**：单例加载 + 分批处理避免 OOM；文本增强策略有依据（核心词前置）；异常隔离做得好。
**缺点**：
- 批次失败时**静默保留无向量数据**，下游 `node_import_milvus` 的校验会发现 `dense_vector` 缺失而终止——错误被推迟到更下游才暴露；
- 单例模型是进程级，多 worker 部署时每个 worker 各自加载一份模型（内存 ×N）；
- 模型加载是同步阻塞的，首次加载慢（CPU 上加载 BGE-M3 可能几十秒）。

### 3.7 node_import_milvus —— Milvus 入库

**干什么**：把带向量的 Chunk 批量写入 Milvus `chunks` 集合，保证幂等（同一商品重导只留最新）。

**怎么做的**（`node_import_milvus.py`）：
1. 校验 chunks 非空、含 `dense_vector`，提取向量维度；
2. `create_collection`：集合不存在则自动建 schema（`chunk_id` 自增主键 + content/title/parent_title/part/file_title/item_name + 双向量）+ HNSW/COSINE 稠密索引 + SPARSE_INVERTED_INDEX/IP 稀疏索引；
3. 幂等：按 `item_name` 删除旧数据（`delete` + `flush`）；
4. `insert` 批量插入，把 Milvus 自动生成的 `chunk_id` 回填到 chunk。

**优点**：自动建表免初始化；幂等策略清晰（按商品名整体覆盖）；索引参数针对 BGE-M3 调过（COSINE + IP 是匹配的）。
**缺点**：
- **"按商品名整体删除再插入"是粗粒度覆盖**：同一个商品名下的文档（比如"华为P60"有 3 本手册）重导一本会删掉另外两本的数据 —— 这是当前最危险的业务缺陷；
- `chunk_id` 回填依赖 insert 返回值与输入顺序一致，批量插入时 Milvus 不保证严格顺序语义，理论上可能错位；
- 无批量大小上限控制，超大文档一次 insert 可能超时。

---

## 4. 在线问答链路逐节点解析

### 4.1 node_item_name_confirm —— 商品名提取与澄清（整条链路的"大脑"）

**干什么**：回答任何问题前，先确认"用户问的是哪个产品"。结合会话历史提取商品名 → 向量检索对齐库内标准名 → 三种结果：确认 / 反问用户 / 拒答。

**怎么做的**（`node_item_name_confirm.py`，7 个 step，单节点 470+ 行）：
1. 从 MongoDB 读最近 10 条历史；
2. 保存用户本次消息（拿到 `message_id`，后面更新用）；
3. `step_3_extract_info`：LLM（JSON 模式）从"当前问题 + 历史"提取 `item_names` 和 `rewritten_query`（把"这个多少钱？"改写为"华为P60 128G 多少钱？"这种独立完整的问题）；
4. `step_4_vectorize_and_query`：对每个提取的商品名用 BGE-M3 生成向量，在 Milvus `item_names` 集合混合检索 Top5；
5. `step_5_align_item_names`：按分数对齐，规则 a>b>c>d：
   - a) 唯一结果 >0.85 → 确认；
   - b) 多个 >0.85 → 优先取与提取名相同的，否则取最高分；
   - c) 无 >0.85 → 取 ≥0.6 的前 5 个作为候选（反问用户）；
   - d) 无 ≥0.6 → 拒答；
6. `step_6_check_confirmation`：确认 → 写入 `item_names`；有候选 → 生成反问句作为 `answer`；都没有 → 生成拒答句；
7. `step_7_write_history`：把反问/拒答写入历史，更新用户消息的 `rewritten_query` / `item_names`。

**优点**：
- **设计亮点**：商品名对齐 + 改写查询这套组合解决了产品文档问答的两大痛点——用户不记得准确型号、问题指代不清（"它怎么用？"）；
- 分数阈值规则清晰、可解释，方便调优；
- 历史消息的 `item_names` 关联会自动回填（`update_message_item_names`），多轮对话的实体能沉淀下来。

**缺点**：
- 单节点 470+ 行、7 个 step 塞在一起，**是当前最大的可维护性债**（应该拆成独立节点：extract / align / confirm）；
- 阈值 0.85/0.6 是拍脑袋的魔法数字，没有评测支撑；
- 反问只给前 3 个候选、查询只取 Top5，产品名多时信息截断；
- 整个节点是**同步串行**的（LLM 提取 → 向量化 → 检索 → 对齐），是整条链路最慢的一环；
- 无缓存：同一商品名每次查询都重新向量化检索。

### 4.2 node_search_embedding —— 普通混合检索

**干什么**：用改写后的完整问题，在 `chunks` 集合里做稠密+稀疏混合向量检索，取 Top5。

**怎么做的**（`node_search_embedding.py`）：
- 问题 BGE-M3 向量化 → 构造 `item_name in [...]` 过滤表达式（限定在已确认商品名下检索）→ `create_hybrid_search_requests`（稠密 COSINE + 稀疏 IP）→ `hybrid_search`，权重 (0.8, 0.2)，归一化评分，Top5，返回 `chunk_id/content/item_name`。

**优点**：过滤表达式把检索范围锁死在已确认商品名下，既提精度又提性能；召回 10 精排 5 的两级结构合理。
**缺点**：权重 0.8/0.2 是经验值；对长查询没有截断策略（超长 query 向量化效果差）；无检索失败降级（Milvus 挂了直接抛异常中断）。

### 4.3 node_search_embedding_hyde —— HyDE 检索

**干什么**：先用 LLM 生成一个"假设的理想答案"（HyDE 文档），把"用户问题 + 假设文档"拼起来做向量检索，缓解短查询与长文档的语义空间不匹配。

**怎么做的**（`node_search_embedding_hyde.py`）：
1. `step_1_create_hyde_doc`：加载 `hyde_prompt`，LLM 生成假设文档；
2. `step_2_search_embedding_hyde`：`rewritten_query + " " + hyde_doc` 拼接 → BGE-M3 向量化 → 带商品名过滤的混合检索 Top5。

**优点**：HyDE 是成熟的 RAG 增强技术，对"怎么操作/如何配置"这类需要完整步骤答案的问题提升明显；与普通检索形成互补（普通检索偏字面相关，HyDE 偏意图相关）。
**缺点**：多一次 LLM 调用（延迟 + 成本）；假设文档质量差时反而引入噪声；与普通检索结果在 RRF 里权重相同（1.0/1.0），没有体现 HyDE 是"辅助"定位。

### 4.4 node_web_search_mcp —— MCP Web 搜索

**干什么**：通过阿里百炼 MCP（Model Context Protocol）的 WebSearch 工具联网搜索，补充本地知识库没有的信息。

**怎么做的**（`node_web_search_mcp.py`）：用 `openai-agents` 的 `MCPServerStreamableHttp` 连接百炼 WebSearch MCP → `call_tool("bailian_web_search", query, count=5)` → 解析返回的 pages → 结构化 `{title, url, snippet}` 列表。

**优点**：MCP 是标准协议，接入/替换搜索服务（换别的 MCP 工具）成本低；结果带 url，可溯源；本地无结果时能兜底。
**缺点**：`asyncio.run()` 在 FastAPI 同步路径里跑（每个请求新建事件循环），并发下效率差；依赖百炼账号；搜索内容未做领域过滤，可能与产品文档冲突（Rerank 时才知道好坏）。

### 4.5 node_rrf —— RRF 融合

**干什么**：把普通检索和 HyDE 检索两路结果按**排名**（不看分数）融合去重排序。

**怎么做的**（`node_rrf.py`）：
- `_as_entity_list`：把 Milvus Hit / 嵌套 dict / 扁平 dict 统一格式化为含 `chunk_id` 的实体列表；
- `reciprocal_rank_fusion`：对每路结果的每个 doc 累计 `weight * 1/(k + rank)`（k=60），按总分降序，取 Top10；`chunk_id` 去重，保留第一次出现的完整文档。

**优点**：RRF 对跨模型、跨检索器的分数不可比问题免疫（只用排名）；实现正确且纯函数化（已写单测覆盖）；去重逻辑安全。
**缺点**：当前只融合两路本地检索（Web 结果绕过 RRF 直接进 Rerank），三路没有统一融合；k=60 与权重 1.0/1.0 是经验值；丢掉了距离分数信息（对本来就该看分数的场景不友好）。

### 4.6 node_rerank —— 重排与动态 TopK

**干什么**：把 RRF 融合后的本地结果 + Web 搜索结果合并，用 BGE Reranker 对"问题-文档"对打分重排，再做动态截断。

**怎么做的**（`node_rerank.py`）：
1. `step_1_merge_docs`：本地 doc（`rrf_chunks`）与 Web doc（`web_search_docs`）统一为 `{text, chunk_id, url, source}` 标准结构，标记 `source=local/web`；
2. `step_2_rerank_docs`：`reranker.compute_score([[question, text]...])` 批量打分 → 按分降序；异常时降级为原顺序（分数 0）；
3. `step_3_topk`：动态截断——最多 10 条、最少 1 条，从第 min 条开始检测"断崖"（相邻分数差 ≥0.5 或相对差 ≥25%）提前截断。

**优点**：Rerank 是 RAG 精度提升的最大单一手段（召回 10 → 精排截断）；动态 TopK 比固定 K 聪明（分数掉崖就少喂几条给 LLM，省 token 且防噪声）；异常降级设计好。
**缺点**：Reranker 模型走本地 CPU（`BGE_RERANKER_DEVICE=cpu`）时 10 条打分也有延迟；断崖阈值同样是无评测拍脑袋；Web 结果无 chunk_id，引用溯源只能靠 url。

### 4.7 node_answer_output —— 答案生成与落库

**干什么**：组装最终 Prompt → LLM 生成答案 → 写回 MongoDB 历史 → SSE 流式推送。

**怎么做的**（`node_answer_output.py`）：
1. `step_1_check_answer`：若 state 已有 answer（反问/拒答分支），直接推送/返回，跳过生成；
2. `step_2_construct_prompt`：把重排文档格式化为 `[1] [local] [chunk_id=..] [score=..] [title=..]` 元数据头+正文，拼接历史、商品名，加载 `answer_out.prompt` 模板；总上下文上限 12000 字符（文档优先，历史被压缩）；
3. `step_3_generate_response`：流式则 `llm.stream` 逐 delta 推 SSE，非流式 `llm.invoke`；失败降级为"抱歉，生成回答时出现错误"；
4. `_extract_images_from_docs`：从重排文档里正则提取图片 URL（MD 图片语法），随答案展示；
5. `step_4_write_history`：答案写回 MongoDB（含 image_urls）。

**优点**：上下文预算管理（12000 字符上限 + 文档优先）务实；引用元数据（chunk_id/url/score）会拼进 Prompt，LLM 可以输出带引用的答案；流式/非流式双模式完整。
**缺点**：历史在上下文超限时会被整段截断，多轮对话的"早期记忆"丢失；没有强制 LLM 输出引用格式（Prompt 软约束，模型可能不遵守）；图片提取只认 MD 语法和 url 后缀，较脆弱。

---

## 5. 基础设施层解析

| 组件 | 作用 | 现状 | 风险 |
|---|---|---|---|
| **Milvus** | 向量库：`chunks` + `item_names` 两集合，混合检索（稠密 COSINE + 稀疏 IP） | 全链路核心，自动建表 | 幂等按商品名整体删，多文档同商品互相覆盖 |
| **MongoDB** | 会话历史（`chat_message` 集合），按 `session_id` + `ts` 建索引 | 读写闭环，SSE/非流式都落库 | 无认证（README 已注明）；无归档/清理策略 |
| **MinIO** | 图片对象存储 | 仅图片使用，上传文件本体不存（README 注明） | 图片 URL 依赖 endpoint 可访问性；无生命周期管理 |
| **Neo4j** | 图数据库 | **只建了连接，零使用**（11 行客户端代码） | 纯占位：Compose 里起了容器、配置齐全，但没有任何写入/查询代码 |
| **FastAPI** | 双服务：导入 8000 / 查询 8011 | 完整（上传/状态/查询/SSE/历史/健康检查） | 任务状态在**进程内存**（`task_utils.py` 全局字典），重启即丢；BackgroundTasks 非持久队列 |
| **LangGraph** | 两条工作流编排 | 完整落地 | 条件路由等逻辑散在节点代码里，图结构简单但节点内部复杂 |

---

## 6. 现有技术选型：优点与缺点

| 选型 | 优点 | 缺点 |
|---|---|---|
| **LangGraph** | 状态机显式化、节点可复用、可流式追踪、天然适合复杂 RAG 编排 | 本项目用成了"串行链"，图的高级能力（checkpoint、持久化、并行分支管理）没用上；学习成本 |
| **BGE-M3** | 单模型同时出稠密+稀疏向量，多语言，Milvus 官方支持好 | 1024 维稠密向量存储/检索成本高；CPU 推理慢 |
| **Milvus 混合检索** | 稠密语义 + 稀疏关键词互补，一个库搞定 | 两个索引两套参数（HNSW/SPARSE_INVERTED），调参面大；无全文检索兜底 |
| **HyDE** | 提升意图类查询召回 | 多一次 LLM 调用；文档质量依赖 LLM |
| **RRF + Rerank** | 组合拳：排名融合抗分数不可比 + 精排提升精度 | 三段都是经验参数（k、权重、阈值），没有评测闭环调参 |
| **MCP WebSearch** | 标准协议、接入成本低、可溯源 | 依赖百炼账号；`asyncio.run` 用法在并发下有隐患 |
| **MinerU** | 解析质量业界领先（表格/公式/版面） | 云 API 依赖；同步轮询阻塞；成本随页数增长 |
| **OpenAI 兼容客户端** | 一套代码换任意模型（DashScope 实测） | 换模型后 Prompt 行为需重测；JSON 模式依赖模型能力 |

---

## 7. POC → MVP 路线图

目标定义：**MVP = 能给别人部署使用的最小版本**（不是论文 demo）。判断标准：别人 `git clone` 后按 README 能跑通、数据不丢、任务可追踪、有基本安全边界、有测试保护。

按"先保命、再增能、后加分"排序：

### 阶段 0：工程化保命（1-2 周）⭐ 最高优先

| # | 事项 | 为什么 | 怎么做 |
|---|---|---|---|
| 0.1 | 补 Dockerfile + docker-compose 编排应用 | 现在只有中间件容器化，应用要手动起 | 两个服务各写 Dockerfile；Compose 加 app 服务；README 一键启动 |
| 0.2 | 任务状态持久化 | 现在 `task_utils` 是内存字典，重启丢任务、多 worker 不共享 | 把任务表落到 MongoDB（task_id/status/done_list），或引入 Celery+Redis |
| 0.3 | 修复"同商品名覆盖"缺陷 | 多文档同型号会互相删数据，是数据正确性问题 | 幂等键从 `item_name` 改为 `item_name + file_title`（或引入文档级 doc_id） |
| 0.4 | 上传文件入 MinIO | 现在文件只落本地磁盘，无持久化 | 上传即传 MinIO，任务里引用 object_key |
| 0.5 | CI 接入 pytest | 现在测试跑在本地 | GitHub Actions：uv sync → pytest（测试已就绪） |

### 阶段 1：可靠性与可观测（1-2 周）

| # | 事项 | 怎么做 |
|---|---|---|
| 1.1 | 统一异常处理与重试 | MinerU 轮询、LLM 调用、Milvus 写入加指数退避重试；关键节点失败时任务标记 failed + 错误入库 |
| 1.2 | 结构化日志 + 指标 | loguru 已有，补 request_id 贯穿；统计每节点耗时、每轮检索/生成延迟 |
| 1.3 | 任务取消与超时治理 | 长任务可取消；导入任务整体超时控制 |
| 1.4 | 配置收敛 | `os.environ.get` 散落各节点，统一走 `app/conf/*` 配置类（部分已完成，收尾） |

### 阶段 2：产品能力补全（2-3 周）

| # | 事项 | 怎么做 |
|---|---|---|
| 2.1 | 文档版本管理 | chunks 加 `version` 字段；重导时按 `doc_id+version` 增量更新；支持回滚（保留旧版本记录） |
| 2.2 | 多租户/用户体系 | 用户表 + `user_id` 贯穿导入/查询；Milvus 过滤条件、MongoDB 历史都加租户维度（schema 已预留 `user_id` 字段，落实即可） |
| 2.3 | 文档管理 API | 列表/删除/重新导入/状态页，不再只是"传了就完" |
| 2.4 | 拆解 node_item_name_confirm | 470 行拆成 extract/align/confirm 三个节点，各自可测可复用 |

### 阶段 3：质量与评测闭环（2-3 周）⭐ 简历亮点

| # | 事项 | 怎么做 |
|---|---|---|
| 3.1 | 构建评测集 | 从 doc/ 的真实手册里人工标注 50-100 条 QA（问题/答案/期望引用段落） |
| 3.2 | 指标管线 | 用评测集算：检索 Recall@5、命中引用率、答案忠实度（LLM-as-judge 打分） |
| 3.3 | 参数调优闭环 | 有了指标后，调 k/权重/阈值/切分长度都有依据，输出一张调参对照表（简历可写"基于评测集调优，召回率 X→Y"） |
| 3.4 | 回归保护 | 每个调参改动跑评测集对比，防回归 |

### 阶段 4：生产加固（按需）

- 鉴权（API Key / JWT）、CORS 白名单、MongoDB 开启认证（README 已警告）；
- HTTPS、密钥管理（Vault / 环境注入而非 .env 提交）；
- 水平扩展：Milvus 集群、查询服务多副本、embedding 模型独立推理服务（避免多进程各自加载）；
- Neo4j 二选一：实现 GraphRAG（实体-关系入库 + 图检索）或彻底移除（消除"有图能力"的误导）。

### 里程碑对照

| 里程碑 | 验收标准 |
|---|---|
| POC（现在） | 两条图跑通、本地 demo 可演示、纯函数有单测 |
| MVP | 阶段 0+1 完成：一键部署、任务可靠、数据不丢、有 CI |
| 可发布 | MVP + 阶段 2+3：版本管理、多租户、评测闭环、安全加固 |

---

## 8. 简历怎么用这个项目

### 8.1 推荐的项目描述（一段话版本）

> **基于 LangGraph 的产品文档 RAG 问答系统**（Python / FastAPI / LangGraph / Milvus / BGE-M3）
>
> 独立设计并实现面向产品手册的 RAG 服务，覆盖"离线入库 + 在线问答"两条 LangGraph 工作流：
> - **离线链路**：MinerU 解析 PDF → 多模态模型生成图片摘要并存储 MinIO → 标题感知切分 → LLM 识别商品名 → BGE-M3 生成稠密/稀疏混合向量 → 幂等写入 Milvus；
> - **在线链路**：先做商品名提取与多轮澄清（反问/拒答），再并行执行普通向量检索、HyDE 检索与 MCP Web 搜索，经 RRF 融合、BGE Reranker 重排后由 LLM 生成带引用答案，支持 SSE 流式输出与 MongoDB 会话记忆；
> - 通过 30+ 个单元测试覆盖切分、融合、向量归一化等核心逻辑，实现 CI 集成。

### 8.2 简历技巧

- **数字要具体**：写"34 个单元测试"、"召回 10 精排 5"、"3 路并行检索"、"10 分钟超时治理"，不要写"实现了 RAG"。
- **突出工程点而非调包**：面试官对"幂等写入""商品名消歧""动态 TopK""上下文预算管理"这类工程决策更感兴趣。
- **诚实标注范围**：可以写"负责设计并实现核心检索链路与问答工作流"，不必强调"整个项目"——但你要能讲清每一层（见第 9 节）。
- **放开源链接**：README 已完善（含架构图），把 GitHub 链接放简历，面试官大概率会看。
- **提前补一个亮点**：如果时间允许，优先做阶段 3 的评测集——"基于 50 条人工标注 QA 调优检索参数"是简历上最有区分度的一句。

---

## 9. 面试高频追问与参考答案

**Q1: 为什么用 LangGraph 而不是直接写 Python 脚本?**
A: 两条链路有明确的"节点 + 状态 + 条件路由"结构（导入链路 7 节点、查询链路 7 节点含并行分支和反问短路）。LangGraph 把状态流转显式化：每个节点只读写 state，方便单测、流式追踪、未来加 checkpoint 持久化。直接用脚本也能跑，但分支逻辑（比如反问直接结束）会散落在 if/else 里，难以维护和可视化。

**Q2: 混合检索里稠密和稀疏向量各解决什么问题?权重为什么 0.8/0.2?**
A: 稠密向量（BGE-M3 1024 维）解决语义相似问题——"怎么换电池"能匹配到"更换电池步骤"；稀疏向量解决关键词精确匹配——型号、编号这类词。0.8/0.2 是经验值，因为产品文档问答以语义匹配为主；理想情况下应该用评测集调（这正是 MVP 阶段 3 要做的事）。诚实承认这一点比编造"经过验证"更有说服力。

**Q3: RRF 和 Rerank 都做排序，为什么不只用一个?**
A: 两者解决的问题不同。RRF 融合的是"不同检索器的排名"（普通检索和 HyDE 的分数体系不可比，只比排名）；Rerank 是交叉编码器，对"问题-文档"逐对打分，精度高但慢（一次只能处理有限条数），所以放最后对 Top10 精排。这是标准的两阶段检索架构：召回阶段求全、重排阶段求准。

**Q4: 商品名确认这步解决什么问题?如果去掉会怎样?**
A: 产品文档问答的特殊性：用户常说不准型号（"华为那款新手机"），且不同型号的配置差异大，检索时如果不过滤商品名，很容易召回别的型号的文档导致答错。这步把问题从"全库检索"变成"指定商品名下检索"，同时通过反问把模糊查询转成明确查询。去掉的话，检索精度和答案准确率都会明显下降，多轮对话的指代（"它怎么用？"）也无法处理。

**Q5: 多轮对话怎么实现的?**
A: 两个机制：一是 MongoDB 存历史，`node_item_name_confirm` 提取商品名时把最近 10 条历史拼进 Prompt，让 LLM 理解指代（"它" → 上一轮确认的商品）；二是改写查询，把指代不完整的问题重写成独立完整的问题，保证后续检索不依赖上下文。答案生成时历史也会作为上下文（有 12000 字符预算，文档优先）。

**Q6: 这个项目最大的技术挑战是什么?**
A: （推荐答案）商品名对齐的准确性——它决定了后续所有检索的质量。难点在于：用户表达和库内标准名不一致、同一商品名多文档共存时的数据覆盖问题、阈值需要评测支撑。其次是任务可靠性——导入任务要能跟踪、失败要可恢复，当前 POC 用内存状态，MVP 阶段已规划持久化。

**Q7: 如果召回结果差，你怎么排查?**
A: 分层排查：① 看 RRF 融合前两路各自的结果（哪路召回差）；② 看切分质量（chunk 是否语义完整、是否过碎）；③ 看商品名过滤是否过严（item_name 匹配不上导致空召回）；④ 看 embedding 的文本增强（item_name 是否前置）；⑤ 最后用评测集量化，而不是靠感觉。这体现工程思维。

**Q8: 图片为什么不做向量化，而是做摘要?**
A: 产品手册图片（面板图、接线图）语义主要靠文字描述，直接图片向量化成本高（多模态 embedding）、且问答时 LLM 无法"看图推理"（纯文本 LLM）。方案是：VLM 生成文字摘要存入向量库参与检索，原始图片存 MinIO，答案生成后把相关图片 URL 一并返回给前端展示。这样检索、生成、展示三层各取所需。

**Q9: 项目里哪些是你觉得设计得不好的地方?**
A: （展示自省能力）① node_item_name_confirm 单节点 470 行，应该拆；② 幂等删除按商品名整体覆盖，多文档同型号会互相覆盖，MVP 阶段 0.3 已列为必修；③ 阈值（0.85/0.6/k=60）全是经验值，没有评测支撑；④ Neo4j 客户端是占位，要么实现 GraphRAG 要么删掉。承认这些并说出改进方案，比完美无瑕更有说服力。

---

*文档生成日期：2026-08-07。代码基线：commit 5a3f60a。修改代码后请同步更新本文档。*
